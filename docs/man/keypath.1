.\" "Generated by swift-argument-parser"
.Dd May 20, 2026
.Dt KEYPATH 1
.Os
.Sh NAME
.Nm keypath
.Nd "KeyPath keyboard remapping  configure, query, control"
.Sh SYNOPSIS
.Nm
.Ar subcommand
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em rule
Manage custom key remappings
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em list
List all custom key remappings
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -enabled-only
Only show enabled rules
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em add
Create or modify a key remapping
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar input
Input key to remap (e.g., caps, lalt)
.It Ar output
Output key for simple remap (e.g., esc, lctl)
.It Fl -action Ar action
Full KeyAction as JSON (e.g., '{"hyper":{}}')
.It Fl -behavior Ar behavior
Full MappingBehavior as JSON
.It Fl -tap Ar tap
Key to emit on tap (for tap-hold)
.It Fl -hold Ar hold
Key to emit on hold (for tap-hold)
.It Fl -timeout Ar timeout
Tap-hold timeout in milliseconds (default: 200)
.It Fl -shifted Ar shifted
Alternate output when shift is held
.It Fl -layer Ar layer
Target layer for this rule (default: base)
.It Fl -title Ar title
Human-readable title for the rule
.It Fl -notes Ar notes
Optional notes/description
.It Fl -apply
Regenerate config and reload Kanata after saving
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em remove
Remove a key remapping
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar input
Input key to remove mapping for
.It Fl -apply
Regenerate config and reload Kanata after saving
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em show
Show details of a custom rule
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar input
Input key to show mapping for
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em collection
Manage rule collections
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em list
List all rule collections
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em create
Create a new rule collection
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name
Name for the new collection
.It Fl -category Ar category
Category (custom, productivity, navigation, layers, accessibility, experimental)
.It Fl -summary Ar summary
Short description of the collection
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em enable
Enable a rule collection
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Collection name or ID
.It Fl -apply
Regenerate config and reload Kanata after saving
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em disable
Disable a rule collection
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Collection name or ID
.It Fl -apply
Regenerate config and reload Kanata after saving
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em show
Show details of a rule collection
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Collection name or ID
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em rename
Rename a rule collection
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Collection name or ID to rename
.It Ar new-name
New name for the collection
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em delete
Delete a rule collection
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Collection name or ID to delete
.It Fl -force
Skip confirmation (required for non-interactive)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em duplicate
Duplicate a rule collection
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Collection name or ID to duplicate
.It Fl -name Ar name
Name for the duplicate (default: '<name> (Copy)')
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em reorder
Move a collection to a new position in the list
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Collection name or ID to move
.It Fl -position Ar position
Target position (0-indexed)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em layer
List or switch Kanata layers
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em list
List available layers
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em create
Create a new layer (with an empty collection targeting it)
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name
Name for the new layer
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em delete
Delete a layer and all its collections
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name
Layer name to delete
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em rename
Rename a layer (updates all collections targeting it)
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name
Current layer name
.It Ar new-name
New layer name
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em switch
Switch to a layer by name
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name
Layer name (e.g., base, nav, vim)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em pack
Manage keyboard packs
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em list
List available packs and their install status
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em show
Show details of a pack
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Pack name, slug, or ID (e.g., 'vim-navigation', 'Home Row Mods')
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em install
Install a pack
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Pack name, slug, or ID (e.g., 'vim-navigation', 'Home Row Mods')
.It Fl -setting Ar setting...
Quick setting value (key=value, repeatable)
.It Fl -apply
Regenerate config and reload Kanata after installing
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em uninstall
Uninstall a pack
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Pack name, slug, or ID (e.g., 'vim-navigation', 'Home Row Mods')
.It Fl -apply
Regenerate config and reload Kanata after uninstalling
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em configure
Update quick settings on an installed pack
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Pack name, slug, or ID (e.g., 'home-row-mods')
.It Fl -setting Ar setting...
Quick setting value (key=value, repeatable)
.It Fl -apply
Regenerate config and reload Kanata after configuring
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em service
Check status and control the Kanata service
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em status
Check system status and health
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -timeout Ar timeout
Timeout in seconds (default: 30)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em start
Start the Kanata service
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em stop
Stop the Kanata service
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em restart
Restart the Kanata service
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em reload
Hot-reload Kanata configuration
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em logs
Show recent Kanata service logs
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -lines Ar lines
Number of lines to show (default: 50)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em config
Inspect and manage Kanata configuration
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em show
Print current generated .kbd configuration
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em path
Print the configuration file path
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em check
Validate configuration using kanata --check
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em apply
Regenerate config and reload Kanata
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em system
Install, repair, or uninstall KeyPath services
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em install
Install KeyPath services and components
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em repair
Repair broken or unhealthy services
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em uninstall
Remove KeyPath services and components
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -delete-config
Also delete user configuration files
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em inspect
Inspect system state without making changes
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em export
Export collections as portable JSON
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em collection
Export a single collection as JSON
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar name-or-id
Collection name or ID to export
.It Fl -output Ar output
Output file path (default: stdout)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em all
Export all collections as JSON
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -output Ar output
Output file path (default: stdout)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em import
Import rules from files or external formats
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em collection
Import a collection from a JSON file
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar path
Path to the JSON file to import
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em karabiner
Import rules from a Karabiner-Elements configuration
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar path
Path to karabiner.json or complex_modifications rule file
.It Fl -collection Ar collection
Merge all rules into a single collection with this name
.It Fl -profile Ar profile
Profile index to import (default: selected profile)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em simulate
Simulate a key sequence and show what Kanata would output
.Pp
Pass keys as space-separated arguments. Append ':hold' to simulate a long press.
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar keys...
Key sequence as space-separated keys (e.g., 'caps a' or 'caps:hold a')
.It Fl -config Ar config
Path to kanata config file (default: active config)
.It Fl -delay Ar delay
Default delay between keys in ms (default: 200)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em help-topics
Extended help and API discovery
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em schemas
List available CLI schemas for agent API discovery
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar noun
Schema noun to inspect (e.g., action, behavior, rule, collection)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em examples
Curated workflow examples for each command noun
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar noun
Noun to show examples for (rule, collection, layer, service, config, system)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em completions
Generate or install shell completions
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.It Em zsh
Generate zsh completions
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em bash
Generate bash completions
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em fish
Generate fish completions
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em install
Install completions for your shell
.Bl -tag -width 6n
.It Fl -shell Ar shell
Shell to install for (zsh, bash, fish). Defaults to current shell from $SHELL.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em install-man
Install man pages for keypath
.Bl -tag -width 6n
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.El
.It Em status
Check system status (shortcut for 'service status')
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -timeout Ar timeout
Timeout in seconds (default: 30)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em remap
Create or modify a key remapping (shortcut for 'rule add')
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar input
Input key to remap (e.g., caps, lalt)
.It Ar output
Output key for simple remap (e.g., esc, lctl)
.It Fl -tap Ar tap
Key to emit on tap (for tap-hold)
.It Fl -hold Ar hold
Key to emit on hold (for tap-hold)
.It Fl -timeout Ar timeout
Tap-hold timeout in milliseconds (default: 200)
.It Fl -apply
Regenerate config and reload Kanata after saving
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em start
Start the Kanata service (shortcut for 'service start')
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em stop
Stop the Kanata service (shortcut for 'service stop')
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em restart
Restart the Kanata service (shortcut for 'service restart')
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em logs
Show service logs (shortcut for 'service logs')
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Fl -lines Ar lines
Number of lines to show (default: 50)
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em unmap
Remove a key mapping and apply (shortcut for 'rule remove --apply')
.Bl -tag -width 6n
.It Fl -json
Force JSON output
.It Fl -no-json
Force human-readable output
.It Fl -dry-run
Preview changes without applying
.It Fl -on-conflict Ar on-conflict
Conflict resolution: fail|replace|skip|merge
.It Ar input
Input key to unmap
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.It Em help
Show subcommand help information.
.Bl -tag -width 6n
.It Ar subcommands...
.It Fl -version
Show the version.
.El
.El
.Sh "EXIT STATUS"
.Ex -std