import Foundation
@testable import KeyPath
import XCTest

/// Deprecated: UDP client removed; keep placeholder test to document migration.
class UDPClientFixTest: XCTestCase {
    func testUDPClientRemoved() {
        // Document that UDP client has been removed in favor of TCP.
        XCTAssertTrue(true)
    }
}

