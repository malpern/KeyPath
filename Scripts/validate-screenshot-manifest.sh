#!/bin/zsh
# validate-screenshot-manifest.sh
# Help content is now served from gh-pages — no bundled screenshot tags to validate.
echo "Help content lives on gh-pages — screenshot manifest check skipped."
exit 0
