<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.keypath.kanata</string>
    
    <!-- SMAppService: program path relative to the app bundle root -->
    <key>BundleProgram</key>
    <string>Contents/Library/KeyPath/kanata-launcher</string>
    
    <!-- Program arguments: program path (required even with BundleProgram) and config -->
    <!-- Note: Config path uses ~ which will be expanded by launchd -->
    <!-- TCP port defaults to 37001, can be overridden via environment if needed -->
    <!-- Debug logging is intentionally disabled in production to prevent log spam -->
    <key>ProgramArguments</key>
    <array>
        <string>Contents/Library/KeyPath/kanata-launcher</string>
        <string>--port</string>
        <string>37001</string>
        <string>--log-layer-changes</string>
    </array>
    
    <key>RunAtLoad</key>
    <true/>
    
    <!-- KeepAlive with SuccessfulExit: false means:
         - Restart if Kanata crashes (exit != 0)
         - Do NOT restart if Kanata exits cleanly (exit == 0)
         This allows the launcher to stop crash loops by exiting cleanly after N failures -->
    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
    </dict>
    
    <key>StandardOutPath</key>
    <string>/var/log/com.keypath.kanata.stdout.log</string>
    
    <key>StandardErrorPath</key>
    <string>/var/log/com.keypath.kanata.stderr.log</string>
    
    <key>SoftResourceLimits</key>
    <dict>
        <key>NumberOfFiles</key>
        <integer>1024</integer>
    </dict>

    <!-- Keyboard input processing is latency-critical — treat this daemon like
         an interactive app so macOS does not throttle CPU, I/O, or timers under load.
         Without this, the daemon runs at Standard priority and the processing thread
         can be starved by background work (compilation, Spotlight indexing), causing
         stuck keys when release events are delayed. -->
    <key>ProcessType</key>
    <string>Interactive</string>

    <!-- Disable macOS timer coalescing. The kanata processing loop uses a 1ms tick
         timer for tap-hold timeouts; coalescing adds unpredictable jitter that can
         extend tap-hold windows long enough for macOS to fire autorepeat. -->
    <key>LegacyTimers</key>
    <true/>

    <!-- If kanata is hung and won't respond to SIGTERM, kill it after 5s instead of
         the default 20s. A keyboard daemon that can't shut down in 5s is stuck. -->
    <key>ExitTimeOut</key>
    <integer>5</integer>

    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>AssociatedBundleIdentifiers</key>
    <array>
        <string>com.keypath.KeyPath</string>
    </array>
</dict>
</plist>
